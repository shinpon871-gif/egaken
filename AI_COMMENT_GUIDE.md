# 🤖 AIコメント機能 設計・仕様・運用ガイド

## 1. 概要

「えがけん」に、OpenAI APIを使用して、投稿された記録に対して自動的に励ましと継続を促すAIコメントを生成する機能を追加しました。
本ドキュメントは、機能仕様、実装詳細、セットアップ、および動作確認手順を統合したものです。

## 2. 機能仕様

### コメント生成ロジック
- **タイミング**: 記録投稿直後に非同期で生成
- **入力情報**: ユーザーのコメント（任意）、練習時間（任意）
- **出力内容**: 40〜80文字程度の日本語テキスト（1〜2文）
- **トーン**: 評価や点数は使わず、励ましと継続を促す。「昨日の自分より一歩前進」というニュアンス。
- **エラーハンドリング**: OpenAI APIの呼び出しに失敗した場合（クォータ不足やサーバーエラー等）、エラーにはせず、予め用意された定型文（「継続は力なり！...」など）をランダムに返却して動作を継続する。

### プロンプト設計（キャラクター設定）
```text
あなたはお絵描きを応援する優しいトレーナーです。
以下の情報をもとに、評価や点数は使わず、励ましと継続を促す短い日本語コメント（1〜2文）を作成してください。

情報：
- ユーザーコメント: {comment}
- 練習時間: {practiceMinutes}分

条件：
- 上から目線にしない
- 批判しない
- 「昨日の自分より一歩前進」というニュアンス
- 40〜80文字程度
```

### UI表示
- **場所**: 記録カード内の下部
- **状態表示**: 
  - 生成中: 「コメント生成中...」
  - 完了後: AIコメントを表示
- **デザイン**: グラデーション背景とロボットアイコン（🤖）で特別感を演出

## 3. アーキテクチャ・実装詳細

### データフロー
1. **User**: 記録投稿フォームで保存ボタン押下
2. **Client**: Firestoreに記録保存（初期値 `aiComment: ''`）
3. **Client**: バックグラウンドで `/api/generate-comment` を呼び出し（ユーザーを待たせない）
4. **Server**: OpenAI API (GPT-4o-mini 等) にリクエスト
5. **Server**: 生成されたコメントをレスポンス
6. **Client**: Firestoreの該当レコードを更新（`aiComment` フィールド）
7. **Client**: リアルタイムリスナー (`onSnapshot`) によりUIが自動更新

### API処理フロー (`app/api/generate-comment/route.ts`)
1. リクエスト受信
2. OpenAI API呼び出し
3. **成功時**: 生成されたテキストを返却
4. **失敗時 (catchブロック)**: 
   - エラーをコンソールに出力
   - 4種類の定型文からランダムに1つ選択して返却 (Status 200)

### ファイル構成
- `app/api/generate-comment/route.ts`: AIコメント生成APIエンドポイント
- `components/CreateRecordForm.tsx`: 記録保存とAPI呼び出しの連携処理
- `components/RecordList.tsx`: AIコメントの表示コンポーネント
- `.env.local`: APIキー管理

### Firestoreスキーマ
`records` コレクションに追加されたフィールド:
```typescript
interface Record {
  userId: string;
  imageUrl: string;
  comment: string;
  practiceMinutes: number;
  aiComment: string; // ← 新規追加: 生成されたコメント
  createdAt: Timestamp;
}
```

## 4. セットアップ手順

### ステップ 1: OpenAI APIキーの取得
1. OpenAI Platform にアクセス
2. API Keys セクションで新しいキーを作成 (`sk-...`)
3. キーをコピーして安全に保管

### ステップ 2: 環境変数の設定
プロジェクトルートの `.env.local` ファイルを作成（または編集）し、キーを設定します。

```bash
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

### ステップ 3: 依存パッケージのインストール
```bash
npm install openai
```

## 5. 動作確認・テスト手順

### 基本テストフロー
1. **開発サーバー起動**: `npm run dev`
2. **ログイン**: Googleアカウントでログイン
3. **記録投稿**: 画像を選択し、コメントと練習時間を入力して「保存」
4. **確認**: 
   - 直後に「コメント生成中...」と表示されること
   - 数秒後にAIからの応援コメントに切り替わること

### テストパターン
| パターン | 入力内容 | 期待される挙動 |
|---|---|---|
| **1. 標準** | コメントあり、時間あり | 内容と時間に触れた具体的な励まし |
| **2. 最小** | コメントなし、時間なし | 一般的な継続への励まし |
| **3. 長文** | 詳細なコメント | 詳細内容を踏まえたフィードバック |

### 検証チェックリスト
- [ ] 記録投稿後にエラーが出ない
- [ ] Firestoreに `aiComment` が保存されている
- [ ] ブラウザのNetworkタブでAPIリクエストが `200 OK` である
- [ ] UIが崩れていない（スマホ含む）

## 6. デプロイ・運用

### Vercelへのデプロイ
本番環境（Vercel）で動作させるには、環境変数の設定が必要です。

1. Vercel Dashboard > Settings > Environment Variables
2. Key: `OPENAI_API_KEY`
3. Value: `sk-...` (取得したキー)
4. Save して Redeploy

### コスト試算
- **モデル**: GPT-4o-mini (推奨)
- **コスト**: 1投稿あたり約 $0.0001 未満
- **目安**: 月間1,000投稿でも $0.10 程度と非常に低コスト

### セキュリティ
- APIキーはサーバーサイド（API Route）でのみ使用し、クライアントバンドルには含めない
- Firestoreセキュリティルールで、他人の記録への書き込みを制限する

## 7. トラブルシューティング

### Q. コメントが「生成中...」のまま変わらない
**原因**: API呼び出しが失敗しているか、APIキーが設定されていません。
**対処**: 
1. `.env.local` に `OPENAI_API_KEY` があるか確認。
2. 開発サーバーを再起動 (`npm run dev`)。
3. ブラウザのコンソールでエラーログを確認。

### Q. エラー「OpenAI APIキーが設定されていません」
**対処**: 環境変数が読み込まれていません。`.env.local` のファイル名と場所を確認し、再起動してください。

### Q. 毎回同じような定型文しか返ってこない
**原因**: OpenAI APIのクォータ不足などでエラーが発生し、フォールバック機能が作動している可能性があります。
**対処**: サーバーのコンソールログを確認し、`AIコメント生成エラー` が出ていないか確認してください。

### Q. 日本語がおかしい、またはトーンが違う
**対処**: `app/api/generate-comment/route.ts` 内の `prompt` 変数を調整してください。

---
**最終更新日**: 2026年2月15日